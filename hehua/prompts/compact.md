Summarize this penetration session transcript for continuation.
Output ≤1200 chars, markdown bullets, MUST preserve ALL of:
- confirmed facts (vulns, creds, open ports, versions, flag progress)
- TRIED list: every concrete path/payload/parameter name already attempted,
  each marked WORKED or DEAD. List actual values (e.g. "param names tried:
  url, target, endpoint, source_url — ALL rejected"), never just categories.
  The continuation MUST NOT repeat anything marked DEAD.
- currently working hypothesis + next 3 concrete actions.
If [durable notes] are provided, merge them verbatim into the output.
No preamble, no extra text.
