# ---- LLM API keys (at least one required, OpenAI-compatible) ----
DEEPSEEK_API_KEY=sk-your-key-here
DEEPSEEK_MODEL=deepseek-chat
LLM_PROVIDER=deepseek

# ---- Optional: GLM-5.2 escalation (stronger model for hard targets) ----
# GLM_API_KEY=
# GLM_MODEL=glm-5.2

# ---- Optional: deep mode (multi-peer parallel pentest) ----
# HEHUA_DEEP=auto
# HEHUA_PEERS=2

# ---- Optional: budget per target (minutes) ----
# HEHUA_PENTEST_BUDGET=30

# ---- Optional: Qwen (阿里百炼 DashScope, OpenAI-compatible) ----
# ALIYUN_API_KEY=
# ALIYUN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# QWEN_MODEL=qwen3-max
