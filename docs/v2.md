# v2 边界清单（8/16 前不动手，出现即记入此处）

1. LLM Planner 动态调度（30s 轮次 + snapshot）——先验 EV 规则版够用
2. ~~一题多 Solver **并行**~~ —— **已实现（深度模式）**：`HEHUA_DEEP=deep/auto` 时每题派
   `HEHUA_PEERS`(默认2) 个 peer 共用靶标、分方向、共享 notes、首解即停；2peer×3容器=6agent
   压满平台 3 容器上限。normal 模式 = 3宽 flash→GLM 渐进。见 HANDOFF「当前架构」。
3. 独立 Observer agent（ralph-loop 注入）——规则看门狗已覆盖 80%
4. 黑板式 Fact/Intent 图（Cairn）——notes 是其降级形态
5. 无头浏览器（XSS bot 类题）——体积/收益不划算
6. MCP 接入、writeup 自学习库、跨题知识沉淀
7. 并发池 >3（平台容器上限 3；现为 2 flash + 1 GLM）
